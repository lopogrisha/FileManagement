# Console File Manager (C++17)

Консольний файловий менеджер, написаний на C++17 із використанням `std::filesystem`.  
Проєкт розбитий на кілька файлів для зручності підтримки та читабельності коду.

---

## 📁 Структура проекту

/project
│
├── AbstractFileManager.h # Абстрактний інтерфейс файлового менеджера
├── FileManager.h # Оголошення класу FileManager
├── FileManager.cpp # Реалізація FileManager
│
├── Utils.h # Допоміжні функції (viewTextFile, selectDrive)
├── Utils.cpp
│
└── Main.cpp # Головна логіка та інтерфейс

markdown
Копировать код

---

## 🧭 Можливості

- Відображення списку файлів і папок
- Навігація стрілками (вгору / вниз)
- Вхід у директорію (Enter)
- Повернення назад (Backspace)
- Видалення файлів та папок (Delete)
- Перегляд текстових файлів у консолі (`.txt`, `.cpp`, `.h`, `.log`)
- Повний набір файлових операцій:

| Клавіша | Дія |
|--------|------|
| **F2** | створити файл |
| **F3** | перейменувати |
| **F4** | копіювати |
| **F5** | перемістити |
| **F6** | показати розмір файлу/папки |
| **F7** | створити папку |
| **F9** | пошук по масці (`*.txt`, `file??.cpp`) |

- Рекурсивні операції (copy, move, size, search)

---

## 🔍 Пошук по масці (F9)

Підтримуються стандартні маски:

- `*` — будь-яка кількість символів
- `?` — один символ

Приклади:

| Маска | Результат |
|-------|-----------|
| `*.txt` | всі `.txt` файли |
| `file??.cpp` | `file01.cpp`, `fileAB.cpp` |
| `data*.*` | файли, що починаються з `data` |
| `*log*` | файли, що містять слово "log" |

Маска автоматично перетворюється у `std::regex`, з урахуванням екранування спецсимволів.

---

## 🛠 Використані технології

- C++17
- `<filesystem>`
- `<regex>`
- `<conio.h>` — читання клавіш без Enter (Windows)
- `system("cls")` — очистка екрану
- `getline(cin >> ws, ...)` — коректний ввід рядків

Програма розрахована на **Windows**, бо Linux/macOS не підтримують `<conio.h>`.

---

## 🏗 Збірка

### MinGW / g++ (Windows)

g++ -std=c++17 Main.cpp FileManager.cpp Utils.cpp -o FileManager

Копировать код

У деяких збірках g++ може знадобитись:

-lstdc++fs

yaml
Копировать код

### Visual Studio

1. Створити Console App (C++)
2. Увімкнути C++17
3. Додати всі `.cpp` і `.h`
4. Зібрати

---

## 🎮 Управління

| Клавіша | Дія |
|---------|-----|
| ↑ / ↓ | переміщення по списку |
| Enter | зайти в директорію / відкрити файл |
| Backspace | повернутися назад |
| Delete | видалити |
| **F2** | створити файл |
| **F3** | перейменувати |
| **F4** | копіювати |
| **F5** | перемістити |
| **F6** | розмір |
| **F7** | створити папку |
| **F9** | пошук по масці |
| Esc | вихід |

---

## 📄 Ліцензія

MIT License  
Код відкритий для використання та модифікацій.
