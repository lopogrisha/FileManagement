# Console File Manager (C++17)

Консольний файловий менеджер, написаний на C++17 із використанням `std::filesystem`.  
Логіка програми розділена на окремі модулі, що робить код чистим, зрозумілим та зручним для підтримки.

## 📁 Структура проекту

/project
│
├── AbstractFileManager.h # Абстрактний інтерфейс файлового менеджера
│
├── FileManager.h # Оголошення класу FileManager
├── FileManager.cpp # Реалізація FileManager
│
├── Utils.h # Допоміжні функції (viewTextFile, selectDrive)
├── Utils.cpp
│
└── Main.cpp # Головний цикл, інтерфейс та обробка клавіш

markdown
Копировать код

## 🧭 Можливості

✔ Перегляд списку файлів і папок  
✔ Навігація стрілками (Up/Down)  
✔ Вхід у папку (Enter)  
✔ Повернення назад (Backspace)  
✔ Перегляд текстових файлів у консолі  
✔ Повноцінні операції з файловою системою:

- **F2** — створити файл  
- **F3** — перейменувати файл/папку  
- **F4** — копіювати файл/папку  
- **F5** — перемістити файл/папку  
- **F6** — дізнатися розмір файлу або папки (рекурсивно)  
- **F7** — створити папку  
- **F9** — пошук по масці (`*.txt`, `file??.log`, `*data*`)  
- **Delete** — видалити файл або папку  
- **Esc** — вихід  

Все працює через `std::filesystem`, включно з рекурсивними операціями.

---

## 🔍 Пошук по масці (F9)

Програма підтримує стандартні файлові маски:

| Маска | Опис |
|------|------|
| `*.txt` | всі файли з розширенням `.txt` |
| `???.cpp` | файли, ім’я яких має рівно 3 символи перед `.cpp` |
| `file*.*` | файли, що починаються з `file` |
| `*name*` | будь-що, що містить слово “name” |

⚙ Маска автоматично конвертується у `std::regex`  
через внутрішню функцію `maskToRegex`, яка:

- `*` → `.*`
- `?` → `.`
- екранує спецсимволи regex (`[](){}+^$|\.`)

---

## 🛠 Використані технології

- **C++17**
- `<filesystem>`
- `<regex>`
- `<vector>`
- `<conio.h>` — обробка клавіш без Enter (Windows)
- `system("cls")` — очистка консолі (Windows)

⚠ Поки що програма працює лише на **Windows**,  
бо Linux не підтримує `<conio.h>`.

---

## 🧑‍💻 Збірка

### 🔧 MinGW / g++ (Windows)

g++ -std=c++17 Main.cpp FileManager.cpp Utils.cpp -o FileManager

Копировать код

У старих версіях g++ може знадобитись:

-lstdc++fs

yaml
Копировать код

### 🧱 Visual Studio

1. Створити **Console Application**
2. Увімкнути стандарт `C++17`
3. Додати всі `.h` та `.cpp` у проект
4. Запустити

---

## 🎮 Управління

| Клавіша | Дія |
|--------|-----|
| ↑ / ↓ | перемістити вибір |
| Enter | зайти в папку / відкрити файл |
| Backspace | вгору по дереву папок |
| Delete | видалити файл або папку |
| **F2** | створити файл |
| **F3** | перейменувати |
| **F4** | копіювати |
| **F5** | перемістити |
| **F6** | розмір |
| **F7** | створити папку |
| **F9** | пошук по масці |
| Esc | вихід |

---

## 🚀 Особливості

- Підтримка **рекурсивних** операцій (copy/move/delete/search)
- Коректна обробка вводу через `getline(cin >> ws, ...)`
- Автоматичне очищення екрану для F-клавіш
- Окремі класи та модулі для чистоти і підтримуваності коду

---

## 📌 TODO (майбутні покращення)

- Кольорове виділення файлів і папок
- Підтримка Linux/macOS (замінити conio.h)
- Відкриття файлів через системні асоціації (WinAPI ShellExecute)
- Підтримка миші
- Панелі 2-віконного вигляду (як Total Commander)

---

## 🏁 Ліцензія

MIT License  
Проєкт відкритий для використання та модифікацій.
